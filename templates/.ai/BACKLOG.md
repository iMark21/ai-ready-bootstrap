# {{PROJECT_NAME}} — Backlog

Stories `{{STORY_PREFIX}}-NNN`. One story = one acceptance feature in `specs/acceptance/`.

## Status convention

- `todo` — pending
- `in-progress` — in flight (only one active per phase)
- `done` — implemented + verified against acceptance
- `parked` — postponed with reason
- `cut` — out of MVP scope

## Active

| ID | Title | Status | Spec | Phase |
|---|---|---|---|---|
| {{STORY_PREFIX}}-001 | (Your first story) | todo | TBD | F0 |

## Out of scope

- (List explicit non-goals here.)

## Next actions

1. Replace `{{PROJECT_NAME}}` and `{{STORY_PREFIX}}` placeholders.
2. Write your first spec following the `spec` command in `.ai/commands/spec.md`.
3. Move `{{STORY_PREFIX}}-001` to `in-progress` and start the loop.
