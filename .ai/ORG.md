# Organization Link

- Registry id: `ai-ready-bootstrap`
- Mode: `standalone`
- Vertical: `other-bets`
- Org: `imark`
- Load order: `agentlayer.yaml` -> `CONTEXT.md` -> `PRODUCT.md` -> `BACKLOG.md` -> `stories/` as needed

This public repository carries a standalone, publishable AI layer. It does not
depend on private org workflow state at runtime. If an org workspace is
available, resolve `org: imark` through machine-level configuration only.
